#!/bin/bash
# Read and display DOOM version from version file

DOOM_DIR="${DOOM_DIR:-$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)}"
VERSION_FILE="$DOOM_DIR/version"

if [[ -f "$VERSION_FILE" ]]; then
    cat "$VERSION_FILE"
else
    echo "unknown"
fi
